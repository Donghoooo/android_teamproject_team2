<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bitc.fullstack503.moneylog_spring.mapper.MainMapper">

    <select id="selectIncomeMain" resultType="bitc.fullstack503.moneylog_spring.dto.IncomeLogDTO" parameterType="String">
        SELECT *
        FROM income_log
        WHERE member_id = "test1"
          AND income_date
        BETWEEN #{year} "-" #{month} "-01"
            AND #{year} "-" #{monthPlus} "-01";
    </select>

    <select id="selectExpenseMain" resultType="bitc.fullstack503.moneylog_spring.dto.ExpenseLogDTO" parameterType="String">
        SELECT *
        FROM expense_log
        WHERE member_id = "test1"
          AND expense_date
            BETWEEN #{year} "-" #{month} "-01"
            AND #{year} "-" #{monthPlus} "-01";
    </select>

</mapper>